generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ---------- Enums ----------

enum TipoUpload {
  CONTACTO_POSVENTA
  CONTACTO_VENTAS
  ENCUESTA_FORD
  FIDELIZACION
  ENCUESTA_FABRICA_VW // Excel de "pendientes" que baja de la plataforma de VW
}

// De qué planilla salió el destinatario de fidelización. Son dos circuitos con
// reglas distintas que conviven en la misma pantalla:
//  - TURNOS: export Ford de agendamientos. Trae el service en "Comentario del
//    Asesor"/"Servicio" y SOLO entran los del 1° al 5° (6° o más queda afuera).
//  - VENTAS: planilla de ventas de la agencia. NO trae ningún dato de service,
//    así que no hay regla de rango: entran todos los Ford 0km de la planilla.
enum OrigenFidelizacion {
  TURNOS
  VENTAS
  MANUAL // alta a mano desde la pantalla, para el cliente que no vino en ninguna planilla
}

// Estado del recordatorio de fidelización por cliente (Parte C: recordatorio de
// service pendiente). No se clasifica la respuesta: es solo un recordatorio.
enum EstadoFidelizacion {
  PENDIENTE // detectado, todavía no se envió el recordatorio
  ENVIADO // recordatorio enviado por WhatsApp
  ERROR // falló el envío (número inválido, template, etc.)
  OMITIDO // no se envió a propósito (opt-out / teléfono suprimido / sin teléfono)
}

// Estado de la encuesta oficial de Ford para un caso (Parte B)
enum EncuestaFordEstado {
  SIN_DATO // nunca vino en un export de Ford
  RESPONDIDA // "Completed"
  PENDIENTE_RESPUESTA // "Invited" / "Reminded": recibió pero no respondió
  NO_ELEGIBLE // "Opt out" / "Quarantine": nunca tuvo chance real de responder
  EMAIL_INVALIDO // "Delivery bounced": la invitación rebotó
}

enum TipoTareaRefuerzo {
  RECORDAR_ENCUESTA // el empleado recuerda al cliente que responda la encuesta
  VERIFICAR_EMAIL // el email rebotó: verificar/corregir y avisar
}

enum EstadoTareaRefuerzo {
  PENDIENTE
  EN_GESTION
  COMPLETADA
  CANCELADA
}

enum ResultadoTareaRefuerzo {
  ENCUESTA_RESPONDIDA
  NO_CONTESTA
  DATOS_ERRONEOS
  RECHAZO
  OTRO
}

enum UploadStatus {
  PROCESANDO
  COMPLETADO
  ERROR
}

enum OrigenAgendamiento {
  DEALER
  FORDPASS
  ONLINEBOOKING
  OTRO
}

enum EstadoContacto {
  PENDIENTE
  ENVIADO
  RESPONDIDO
  NO_RESPONDIO
  INTERNO
  ERROR
  // No contestó ninguno de los dos WhatsApp: lo tiene que llamar Calidad.
  // En pantalla se lee "3° contacto pendiente", que es el nombre con el que lo
  // pidió Calidad: el 1° es el WhatsApp inicial, el 2° la insistencia y el 3°
  // la llamada. El valor guardado no se renombra para no migrar datos que ya están.
  LLAMADA_PENDIENTE
  // Contestó, pero por teléfono. Se mide aparte para saber cuántos se rescatan
  // llamando y cuántos contestan solos.
  RESPONDIO_LLAMADA
  // Se agotaron los tres contactos: no contestó el WhatsApp inicial, no contestó
  // la insistencia y tampoco se lo pudo agarrar por teléfono.
  //
  // Es DISTINTO de NO_RESPONDIO, y la diferencia importa: aquel es "se le
  // escribió una vez y no contestó", que en las marcas sin circuito de
  // insistencia es todo lo que se sabe. Este es "se intentó todo lo que el
  // sistema sabe hacer y a esta persona no se la puede alcanzar", que es una
  // conclusión, no un vencimiento de plazo.
  NO_RESPONDE_CONTACTOS
}

enum MessageDirection {
  ENTRANTE
  SALIENTE
}

enum Semaforo {
  VERDE
  AMARILLO
  ROJO
}

// Gravedad del malestar del cliente, independiente de la confianza del modelo
// en la clasificación. Decide (en código) si un AMARILLO abre RQR automático.
enum Severidad {
  LEVE
  MODERADA
  GRAVE
}

enum EstadoRQR {
  ABIERTO
  EN_TRATAMIENTO
  CERRADO
}

enum RolUsuario {
  ADMIN
  CALIDAD
  // Puesto acotado: solo Fidelizacion y el Seguimiento de esos clientes. No ve
  // Contacto Posterior, ni RQR, ni tableros. Que provincia trabaja lo sigue
  // decidiendo Usuario.sucursal, igual que para CALIDAD, asi que manana se puede
  // crear el mismo rol para otra provincia sin tocar codigo.
  FIDELIZACION
}

// Motivo por el que un teléfono queda en la lista de supresión (no contactar).
enum MotivoSupresion {
  OPT_OUT_WHATSAPP // el cliente respondió BAJA/STOP por WhatsApp (automático)
  SOLICITUD_MANUAL // pidió la baja por teléfono/presencial (lo carga el ADMIN)
  NUMERO_INVALIDO // número inexistente/erróneo
  OTRO
}

// Tipo de valor que normaliza un alias declarado por el ADMIN.
enum TipoAlias {
  ASESOR
  SUCURSAL
  // Nombre de vendedor de Volkswagen -> su código de 7 dígitos. Lo necesita el
  // Excel interno de encuestas, que trae el nombre y no el código.
  VENDEDOR_VW
}

// Área del negocio a la que pertenece un caso/RQR (datos separados).
enum AreaTrabajo {
  VENTAS
  POSVENTA
}

// Área que puede ver/gestionar un usuario. AMBAS = sin restricción (ve las dos).
enum AreaUsuario {
  VENTAS
  POSVENTA
  AMBAS
}

// ---------- Modelos ----------

model ExcelUpload {
  id            String       @id @default(cuid())
  tipo          TipoUpload   @default(CONTACTO_POSVENTA)
  filename      String
  sucursal      String // ej: "San Juan", "Mendoza"
  periodo       String // ej: "2026-07"
  uploadedBy    String
  uploadedAt    DateTime     @default(now())
  columnMapping Json
  kpiResumen    Json? // KPIs de las filas de resumen del Excel original, para contrastar contra lo que calcule el sistema
  totalRows     Int
  status        UploadStatus @default(PROCESANDO)

  // Borrado lógico: nunca se borra físicamente (los datos quedan recuperables).
  // Los listados excluyen las filas con eliminadoEn seteado.
  eliminadoEn    DateTime?
  eliminadoPorId String?
  eliminadoPor   Usuario?  @relation("ExcelUploadEliminadoPor", fields: [eliminadoPorId], references: [id])

  casos                Caso[]
  tareasRefuerzo       TareaRefuerzo[]
  clientesFidelizacion ClienteFidelizacion[]
  encuestasFabricaVW   EncuestaFabricaVW[]

  @@index([sucursal, periodo])
  @@index([eliminadoEn])
}

// Destinatario de un recordatorio de Fidelización. Recibe UN mensaje por
// WhatsApp (plantilla "fidelizacion_posventa"). NO se clasifica la respuesta ni
// se crea un Caso: es un flujo aparte, solo recordatorio. Cada fila es un
// destinatario. Sale de una de las dos planillas (ver OrigenFidelizacion).
model ClienteFidelizacion {
  id       String      @id @default(cuid())
  uploadId String
  upload   ExcelUpload @relation(fields: [uploadId], references: [id], onDelete: Cascade)

  origen           OrigenFidelizacion @default(TURNOS)
  nombre           String
  telefono         String // el mejor teléfono contactable (E.164 si normalizó; si no, el crudo)
  telefonosNorm    String[] // teléfonos normalizados (para chequear la lista de supresión)
  modelo           String?
  patente          String?
  asesor           String? // asesor de servicio (TURNOS) o vendedor (VENTAS)
  // El service pendiente detectado (1 a 5). NULL en los de origen VENTAS: esa
  // planilla no dice qué service le toca al cliente.
  numeroServicio   Int?
  comentarioAsesor String?            @db.Text // el texto original de donde se detectó (solo TURNOS)
  // Fecha de entrega del vehículo (solo VENTAS): es el único dato temporal que
  // trae esa planilla y sirve para ordenar y para entender a quién se contactó.
  fechaEntrega     DateTime?
  sucursal         String?

  estado         EstadoFidelizacion @default(PENDIENTE)
  waMessageId    String? // id del mensaje en Meta (si se envió)
  error          String? // motivo del fallo/omisión (si ERROR/OMITIDO)
  enviadoEn      DateTime?
  // El cliente apretó el botón "Agendar mi Turno con un Asesor": queda pendiente
  // de que un asesor lo llame. Se resalta en Seguimiento. null = no lo pidió.
  quiereAsesorEn DateTime?
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt

  // Borrado lógico (solo ADMIN); los listados excluyen los eliminados.
  eliminadoEn    DateTime?
  eliminadoPorId String?
  eliminadoPor   Usuario?  @relation("ClienteFidelizacionEliminadoPor", fields: [eliminadoPorId], references: [id])

  // Conversación: recordatorio saliente + respuestas del cliente (se ven en
  // Seguimiento igual que las de un Caso).
  mensajes WhatsappMessage[]

  @@index([uploadId])
  @@index([estado])
  @@index([origen])
  @@index([eliminadoEn])
}

// Cada fila del seguimiento post-servicio ("Contacto Posterior")
model Caso {
  id                     String             @id @default(cuid())
  uploadId               String
  upload                 ExcelUpload        @relation(fields: [uploadId], references: [id], onDelete: Cascade)
  numeroOrden            String
  fechaProgramacion      DateTime
  hora                   String?
  // Área del caso (POSVENTA para el Contacto Posventa; VENTAS para Contacto
  // Ventas). Decide qué usuarios lo ven/gestionan.
  area                   AreaTrabajo        @default(POSVENTA)
  origenAgendamiento     OrigenAgendamiento @default(DEALER)
  // asesor = nombre NORMALIZADO para mostrar/agrupar (ej. "Carla Campora").
  // asesorCodigo = identificador numérico si venía en el texto (ej. "140445"),
  // guardado aparte para desambiguar homónimos. asesorRaw = valor original del
  // Excel, para trazabilidad y para poder re-normalizar en el futuro.
  asesor                 String
  asesorCodigo           String?
  asesorRaw              String?
  modelo                 String
  patente                String
  chasisVIN              String?
  nombrePropietario      String
  emailPropietario       String?
  celular                String
  whatsapp               String
  // Teléfonos normalizados a E.164 (whatsapp + celular, deduplicados). Se usa
  // para cruzar contra la lista de supresión sin depender del formato original.
  telefonosNorm          String[]           @default([])
  comentarioAsesor       String?            @db.Text
  fechaSalida            DateTime?
  diasEnServicio         Int?
  sucursal               String
  sucursalRaw            String?
  estadoContacto         EstadoContacto     @default(PENDIENTE)
  ultimoErrorEnvio       String? // motivo del último fallo de envío de WhatsApp
  // Cuándo salió el SEGUNDO contacto (Volkswagen). Sirve para no mandarlo dos
  // veces y para contar las 24 h hasta pasar el caso a llamada pendiente. No
  // alcanza con mirar el último saliente: podría ser cualquier otro mensaje.
  segundoContactoEn      DateTime?
  // Por qué no se pudo cerrar por teléfono (no atendió, número equivocado, no
  // quiso contestar). Sin esto, un caso cerrado no se distingue del que nunca
  // se intentó llamar.
  llamadaMotivo          String?
  tieneRqrAbierto        Boolean            @default(false) // true mientras exista un RQR ABIERTO/EN_TRATAMIENTO del caso
  // Cuándo se le mandaron las 5 preguntas de la encuesta de Posventa. Sirve para
  // saber que el próximo mensaje del cliente las está contestando, y para no
  // mandárselas dos veces si vuelve a apretar el botón.
  encuestaItemsEnviadaEn DateTime?
  // ¿A este auto le hicieron lavado en esta visita? (Volkswagen)
  //
  // Viene del Excel de Contacto Posventa: la columna "Tipo de visita" trae una
  // letra y un número, y el número es el que importa (1 = se lavó, 0 = no).
  //
  // Decide UNA cosa concreta: si al cliente se le pregunta o no cómo le
  // entregaron el auto de limpieza. Preguntarle por un lavado que no le hicieron
  // es la clase de detalle que le dice al cliente que el mensaje es automático y
  // que nadie miró su caso.
  //
  // null = no se sabe (archivos viejos, o la fila vino sin número). Se comporta
  // como antes: se le pregunta por el lavado. Es a propósito: perder la pregunta
  // por un dato que faltó sería perder información de todos los casos.
  tuvoLavado             Boolean?
  // El cliente apretó el botón "Quiero participar por Llamada": pidió que lo
  // llamen en vez de contestar por WhatsApp. Queda pendiente de que alguien de
  // Calidad lo llame, y se resalta en Seguimiento.
  //
  // Es el mismo patrón que ClienteFidelizacion.quiereAsesorEn. Sin esto, ese
  // botón caía como un mensaje cualquiera: la IA lo clasificaba como si fuera
  // una opinión y al cliente le llegaba el mensaje automático equivocado.
  quiereLlamadoEn        DateTime?
  createdAt              DateTime           @default(now())

  // Parte A: agradecimiento + recordatorio de encuesta post-clasificación
  agradecimientoEnviadoEn DateTime? // se envía una sola vez por caso; null = no enviado
  whatsappOptOut          Boolean   @default(false) // el cliente pidió la baja (BAJA/STOP): no más automatismos

  // Parte B: seguimiento de la encuesta oficial de Ford
  encuestaFordEstado EncuestaFordEstado @default(SIN_DATO)
  encuestaFordFecha  DateTime? // "Fecha respuesta" del export de Ford, cuando existe

  // Borrado lógico (solo ADMIN); los listados excluyen los eliminados.
  eliminadoEn    DateTime?
  eliminadoPorId String?
  eliminadoPor   Usuario?  @relation("CasoEliminadoPor", fields: [eliminadoPorId], references: [id])

  mensajes           WhatsappMessage[]
  analisis           SentimentAnalysis[]
  // Puntajes por ítem de la encuesta de Posventa (Volkswagen). Vacío en Ford
  // y en los casos de Ventas.
  evaluacionPosventa EvaluacionPosventa[]
  rqrs               RQR[]
  tareasRefuerzo     TareaRefuerzo[]
  supresiones        SupresionContacto[]  @relation("SupresionCasoOrigen")
  avisos             Aviso[]
  // Si el caso es un promotor de Posventa (5 estrellas) en la pestaña de
  // Encuestas de fábrica PV (Volkswagen). Ver EncuestaFabricaPV.
  encuestaFabricaPV  EncuestaFabricaPV?

  @@index([uploadId])
  @@index([sucursal, estadoContacto])
  @@index([numeroOrden])
  @@index([eliminadoEn])
  @@index([encuestaFordEstado])
  @@index([area])
}

model WhatsappMessage {
  id                    String               @id @default(cuid())
  // Un mensaje pertenece a un Caso (Contacto Posventa/Ventas) O a un
  // ClienteFidelizacion (recordatorio de Fidelización): exactamente uno de los
  // dos. Por eso ambos son opcionales (antes casoId era obligatorio).
  casoId                String?
  caso                  Caso?                @relation(fields: [casoId], references: [id], onDelete: Cascade)
  clienteFidelizacionId String?
  clienteFidelizacion   ClienteFidelizacion? @relation(fields: [clienteFidelizacionId], references: [id], onDelete: Cascade)
  direction             MessageDirection
  content               String               @db.Text
  templateName          String?
  waMessageId           String?
  status                String
  // true = mensaje de agradecimiento/recordatorio automático (Parte A),
  // para distinguirlo del template inicial en reportes y en el detalle del caso
  esAgradecimiento      Boolean              @default(false)
  // QUIÉN lo escribió, cuando lo escribió una persona desde Seguimiento.
  // Null en los entrantes y en todo lo automático (plantilla de contacto,
  // agradecimiento, respuesta al botón de fidelización).
  //
  // Se guarda ACÁ y no solo en la auditoría porque la pregunta "¿quién le mandó
  // esto al cliente?" aparece mirando la conversación, y desde ahí no había
  // forma de contestarla: el registro de auditoría no se puede buscar por
  // cliente ni por caso, y su detalle guarda el número de orden y el largo del
  // texto, no el nombre de quien escribió.
  enviadoPorId          String?
  enviadoPor            Usuario?             @relation("MensajeEnviadoPor", fields: [enviadoPorId], references: [id])
  // Media entrante (nota de voz, imagen, etc.): el id del archivo en Meta, su
  // mime y el tipo. Se guarda porque el análisis corre async (separado del
  // webhook) y necesita bajar el audio de Meta para que Gemini lo transcriba.
  // Para audio, el `content` arranca como "[audio]" y se reemplaza por la
  // transcripción cuando el worker la obtiene.
  mediaId               String?
  mediaMimeType         String?
  mediaTipo             String? // "audio" | "image" | "document" | "video" (el type de Meta)
  // Marca que este mensaje ENTRANTE ya fue cubierto por un análisis. Una misma
  // tanda de mensajes del cliente se analiza JUNTA y en una sola llamada a la
  // IA, así que varios mensajes pueden compartir un único SentimentAnalysis:
  // esta fecha es lo que evita volver a analizarlos.
  analizadoEn           DateTime?
  createdAt             DateTime             @default(now())

  analisis SentimentAnalysis[]
  // Puntajes por ítem de la encuesta de Posventa (Volkswagen). Vacío en Ford y
  // en los casos de Ventas.

  // Único: el id de Meta identifica la entrega; ante un reintento del webhook
  // (Meta reenvía la misma notificación) esto impide duplicar el mensaje
  // entrante y volver a disparar el análisis. Los salientes con waMessageId
  // NULL no colisionan (en Postgres los NULL son distintos entre sí).
  @@unique([waMessageId])
  @@index([casoId])
  @@index([clienteFidelizacionId])
  @@index([enviadoPorId])
}

model SentimentAnalysis {
  id                     String           @id @default(cuid())
  casoId                 String
  caso                   Caso             @relation(fields: [casoId], references: [id], onDelete: Cascade)
  // Nullable: los análisis históricos importados desde Excel no tienen mensaje de WhatsApp asociado
  messageId              String?
  message                WhatsappMessage? @relation(fields: [messageId], references: [id], onDelete: Cascade)
  // Nullable: si la IA no devuelve JSON interpretable, el registro queda
  // sin semáforo y con requiereRevisionManual=true para que Calidad lo clasifique
  semaforo               Semaforo?
  // Gravedad del malestar (LEVE/MODERADA/GRAVE), independiente de la confianza.
  // La confianza sigue guardándose como señal de calidad, pero ya NO decide el RQR.
  severidad              Severidad?
  // Puntaje de 1 a 5 estrellas (5 = mejor). Lo usan las marcas que miden por
  // estrellas en vez de semáforo (Volkswagen); en Ford queda null.
  // El semáforo y la severidad se DERIVAN de este puntaje (ver
  // derivarDeEstrellas en sentiment.service.ts), así todo lo que ya funciona
  // por semáforo —avisos, agradecimientos, filtros, apertura de RQR— sigue
  // andando igual sin tocar nada, y las pantallas muestran las estrellas.
  estrellas              Int?
  confianza              Float
  // Las causas raíz que ve la IA (o que corrigió una persona). Una LISTA desde el
  // 17-09-2026: un cliente se puede quejar de más de una cosa. Vacía = sin causa.
  categoriasCausaRaiz    String[]         @default([])
  resumenIA              String           @db.Text
  respuestaCrudaIA       Json
  requiereRQR            Boolean          @default(false)
  requiereRevisionManual Boolean          @default(false)
  esHistoricoImportado   Boolean          @default(false)
  // El análisis salió de una LLAMADA que cargó una persona, no de la IA.
  esLlamada              Boolean          @default(false)
  // Un caso tiene UN análisis que cuenta (esSeguimiento=false). Los mensajes
  // que el cliente manda después quedan como seguimiento y NO suman a las
  // estadísticas ni a los rankings: si no, un cliente conversador contaba como
  // varias respuestas. Un seguimiento PEOR que el vigente lo reemplaza
  // (escalada) y el anterior pasa a ser seguimiento.
  esSeguimiento          Boolean          @default(false)
  // Cuántos mensajes del cliente se consolidaron en este análisis.
  mensajesAnalizados     Int              @default(1)
  analyzedAt             DateTime         @default(now())

  rqr                RQR?
  evaluacionPosventa EvaluacionPosventa[]

  @@index([casoId])
  @@index([semaforo])
  @@index([casoId, esSeguimiento])
}

// Mensajes entrantes de WhatsApp que no matchean con ningún Caso
// (ej: alguien responde desde otro número). Se conservan para revisión manual.
model MensajeHuerfano {
  id          String   @id @default(cuid())
  telefono    String
  waMessageId String?
  content     String   @db.Text
  payload     Json?
  receivedAt  DateTime @default(now())

  @@index([telefono])
}

// Formulario formal de Reclamo/Queja/Reporte.
// Se genera automáticamente ante semáforo AMARILLO/ROJO, o manualmente.
model RQR {
  id                  String             @id @default(cuid())
  // Nullable: un RQR creado a mano (reclamo telefónico/presencial) puede no
  // tener Caso en el sistema; en ese caso se usan los campos *Manual
  casoId              String?
  caso                Caso?              @relation(fields: [casoId], references: [id])
  nombreClienteManual String?
  telefonoManual      String?
  modeloManual        String?
  // El cliente NO quiso identificarse. Es distinto de "no cargaron el nombre":
  // sin este campo las dos cosas se ven igual (nombre vacío) y no se puede saber
  // si falta un dato o si el reclamo es anónimo a propósito. Con esto, el
  // formulario no exige nombre y en pantalla dice "Anónimo" en vez de
  // "(sin datos)", que parece un error de carga.
  clienteAnonimo      Boolean            @default(false)
  sentimentAnalysisId String?            @unique
  sentimentAnalysis   SentimentAnalysis? @relation(fields: [sentimentAnalysisId], references: [id])
  // Área del RQR: hereda la del Caso; para los RQR manuales sin caso se elige
  // al crearlo (un usuario restringido solo puede elegir la suya).
  area                AreaTrabajo        @default(POSVENTA)
  numeroRQR           String             @unique // correlativo por año, ej "RQR-2026-0042" (lo genera el servicio)
  fechaApertura       DateTime           @default(now())
  canal               String             @default("Posventa")
  areaOrigen          String // ej "Taller", "Ventas"
  areaAfectada        String?
  asesor              String
  descripcionReclamo  String             @db.Text // pre-completado desde resumen IA, editable
  tratamientoBitacora String?            @db.Text // lo completa Calidad
  solucionPropuesta   String?            @db.Text
  tratamientoDadoPor  String?
  fechaCierre         DateTime?
  observaciones       String?            @db.Text
  responsableCierre   String?
  // Las causas raíz del reclamo: sugeridas por la IA, confirmables y editables por
  // Calidad. Varias desde el 17-09-2026 (pedido del dueño, las dos marcas): todas
  // valen igual y en los reportes el RQR suma en cada una. Vacía = sin clasificar.
  causasRaiz          String[]           @default([])
  estado              EstadoRQR          @default(ABIERTO)

  // ---- Campos del RQR de Volkswagen (quedan null en Ford) ----
  // VW clasifica el reclamo con más detalle que Ford. Todos son opcionales para
  // que el mismo modelo sirva a las dos marcas; el formulario los pide (y el
  // controller los exige) solo cuando la marca los usa.
  //
  // OJO con `tipoContacto` vs `area`: `area` (AreaTrabajo) es lo que gobierna
  // QUIÉN puede ver el RQR y solo tiene VENTAS/POSVENTA, así que se sigue
  // usando para eso. `tipoContacto` es el dato de negocio de VW y suma
  // PLAN_DE_AHORRO, que no existe como área de usuario.
  // POR QUÉ SON DOS COSAS DISTINTAS (decisión de Calidad de VW):
  //  - tipoContacto: por qué TEMA se contactó el cliente.
  //  - areaPrincipal + subareas: qué SECTOR es responsable del reclamo.
  // Pueden no coincidir: el cliente llama por un tema de posventa y el problema
  // termina siendo de Plan de Ahorro.
  tipoContacto        String? // VENTAS | POSVENTA | PLAN_DE_AHORRO (ver config/areas-vw.ts)
  areaPrincipal       String? // VENTAS | POSVENTA | PLAN_DE_AHORRO — el sector responsable
  // VARIAS desde el 17-09-2026 (pedido del dueño): un mismo reclamo puede tocar
  // más de una subárea del mismo sector (ej. "Demora en la entrega" y "Falta de
  // información"). Antes era una sola y había que elegir la que más pesara.
  subareas            String[] @default([]) // códigos de subárea DENTRO de areaPrincipal
  origenRqr           String? // EMAIL | TELEFONO | WHATSAPP | PERSONALMENTE
  // Datos del concesionario donde ocurrió el hecho.
  codigoSucursal      String? // 4 caracteres, viene de la carga de casos
  razonSocial         String?
  // El tratamiento puede quedar a cargo de hasta dos personas.
  tratamientoDadoPor2 String?
  // Quién cargó el RQR (sobre todo para los manuales). Es dato del formulario,
  // distinto de la auditoría: acá se ve en el documento que se imprime.
  creadoPorId         String?
  creadoPor           Usuario? @relation("RQRCreadoPor", fields: [creadoPorId], references: [id])
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  // Borrado lógico (solo ADMIN); los listados excluyen los eliminados.
  eliminadoEn    DateTime?
  eliminadoPorId String?
  eliminadoPor   Usuario?  @relation("RQREliminadoPor", fields: [eliminadoPorId], references: [id])

  avisos Aviso[]

  @@index([casoId])
  @@index([estado])
  @@index([eliminadoEn])
  @@index([area])
}

model Usuario {
  id                   String      @id @default(cuid())
  nombre               String
  email                String      @unique
  passwordHash         String
  rol                  RolUsuario  @default(CALIDAD)
  // Área que ve/gestiona. AMBAS = sin restricción. El ADMIN ignora esto (ve todo).
  area                 AreaUsuario @default(AMBAS)
  // Provincia/sucursal que atiende (ej. "San Juan", "Mendoza"). null = TODAS.
  // Se usa para repartir los refuerzos Ford: una tarea de un caso de Mendoza no
  // se le asigna a alguien de San Juan. El ADMIN no está limitado por esto.
  sucursal             String?
  activo               Boolean     @default(true)
  // Parte B: participa del reparto equitativo de tareas de refuerzo.
  // La admin suele quedar en false (supervisa, no gestiona casos).
  participaEnRefuerzos Boolean     @default(true)
  createdAt            DateTime    @default(now())
  // Borrado lógico. Un Usuario cuelga de once relaciones (auditoría, RQR que
  // creó, mensajes que envió): borrarlo de verdad se llevaría la trazabilidad.
  // Al eliminarlo se le libera el email, para poder reusarlo en una cuenta nueva.
  eliminadoEn          DateTime?

  auditLogs                AuditLog[]
  casosEliminados          Caso[]                @relation("CasoEliminadoPor")
  fidelizacionesEliminadas ClienteFidelizacion[] @relation("ClienteFidelizacionEliminadoPor")
  rqrsEliminados           RQR[]                 @relation("RQREliminadoPor")
  rqrsCreados              RQR[]                 @relation("RQRCreadoPor")
  uploadsEliminados        ExcelUpload[]         @relation("ExcelUploadEliminadoPor")
  tareasAsignadas          TareaRefuerzo[]       @relation("TareaAsignada")
  supresionesCreadas       SupresionContacto[]   @relation("SupresionCreadaPor")
  aliasCreados             AliasNormalizacion[]  @relation("AliasCreadoPor")
  avisosVistos             Aviso[]               @relation("AvisoVistoPor")
  mensajesEnviados         WhatsappMessage[]     @relation("MensajeEnviadoPor")
}

// Lista de supresión POR TELÉFONO: quien está acá nunca recibe una campaña de
// WhatsApp, sin importar cuántos casos nuevos genere (a diferencia del flag
// whatsappOptOut, que es por caso y se pierde al re-importar el mes siguiente).
model SupresionContacto {
  id           String          @id @default(cuid())
  telefono     String          @unique // E.164 normalizado (+549...)
  motivo       MotivoSupresion
  casoOrigenId String? // el caso donde se originó la baja, si aplica
  casoOrigen   Caso?           @relation("SupresionCasoOrigen", fields: [casoOrigenId], references: [id])
  creadoPorId  String? // null = supresión automática del sistema (opt-out por WhatsApp)
  creadoPor    Usuario?        @relation("SupresionCreadaPor", fields: [creadoPorId], references: [id])
  notas        String?         @db.Text
  creadoEn     DateTime        @default(now())

  @@index([motivo])
  @@index([creadoEn])
}

// Alias declarado por el ADMIN: "C. Campora" (claveOrigen) equivale a "Carla
// Campora" (valorCanonico). Se aplica en reportes/rankings y en importaciones
// futuras. claveOrigen es la clave normalizada (sin acentos, minúsculas).
model AliasNormalizacion {
  id             String    @id @default(cuid())
  tipo           TipoAlias
  claveOrigen    String // clave normalizada del valor variante
  valorCanonico  String // nombre que se muestra/agrupa
  codigoCanonico String? // código del asesor canónico, si aplica
  creadoPorId    String?
  creadoPor      Usuario?  @relation("AliasCreadoPor", fields: [creadoPorId], references: [id])
  creadoEn       DateTime  @default(now())

  @@unique([tipo, claveOrigen])
  @@index([tipo])
}

// Configuración editable del sistema (clave→valor). Parte A guarda acá los
// textos de los mensajes automáticos y sus toggles.
model Configuracion {
  clave         String   @id
  valor         String   @db.Text
  actualizadoEn DateTime @updatedAt
}

// Parte B: tarea de refuerzo de la encuesta de Ford, gestionada por un HUMANO
// (el sistema no contacta al cliente; solo organiza y registra).
model TareaRefuerzo {
  id             String                  @id @default(cuid())
  casoId         String
  caso           Caso                    @relation(fields: [casoId], references: [id], onDelete: Cascade)
  tipo           TipoTareaRefuerzo
  asignadoAId    String?
  asignadoA      Usuario?                @relation("TareaAsignada", fields: [asignadoAId], references: [id])
  estado         EstadoTareaRefuerzo     @default(PENDIENTE)
  resultado      ResultadoTareaRefuerzo?
  notas          String?                 @db.Text
  creadaEn       DateTime                @default(now())
  actualizadaEn  DateTime                @updatedAt
  completadaEn   DateTime?
  origenUploadId String
  origenUpload   ExcelUpload             @relation(fields: [origenUploadId], references: [id])

  @@index([asignadoAId, estado])
  @@index([casoId])
  @@index([estado])
}

// Registro de auditoría: quién hizo qué, cuándo y desde dónde.
// TABLA DE SOLO INSERCIÓN: el sistema no expone ningún endpoint ni método
// para actualizar o eliminar filas de acá (ni siquiera ADMIN). Una corrección
// se hace agregando un registro nuevo, nunca editando el original.
model AuditLog {
  id        String   @id @default(cuid())
  // Nullable: acciones del sistema (cron, jobs) no tienen usuario humano
  usuarioId String?
  usuario   Usuario? @relation(fields: [usuarioId], references: [id])
  accion    String // ej "LOGIN", "LOGIN_FALLIDO", "CASO_ELIMINADO", "RQR_CERRADO"
  entidad   String // ej "Caso", "RQR", "Usuario", "Auth"
  entidadId String? // id de la entidad afectada, cuando aplica
  // Estado antes/después u otro contexto. NUNCA contraseñas, tokens ni API keys.
  detalles  Json?
  ip        String
  userAgent String
  createdAt DateTime @default(now())

  @@index([usuarioId])
  @@index([accion])
  @@index([entidad])
  @@index([ip])
  @@index([createdAt])
}

// ---------- Avisos en pantalla ----------
// Reemplazan al mail: cuando un caso necesita que alguien lo agarre (se abrió
// un RQR, el cliente escaló después de haber respondido bien, quedó un
// amarillo sin RQR), se crea un aviso y la app lo muestra como cartel rojo
// arriba de todo, en TODAS las pantallas, hasta que se lo marca visto o el RQR
// se cierra. Así nadie se entera tarde por no haber entrado a la pantalla justa.

enum TipoAviso {
  RQR_ABIERTO // se abrió un RQR automático a partir de la respuesta del cliente
  ESCALADO // el cliente ya había sido clasificado y una respuesta posterior empeoró el semáforo
  AMARILLO_SIN_RQR // semáforo amarillo que no llegó a abrir RQR, pero conviene mirar
  REVISION_MANUAL // el cliente respondió algo que la IA no pudo clasificar: mirarlo antes de las 24 h
  // Dos casos del MISMO auto/cliente con números de orden distintos: puede ser
  // un error de carga o dos visitas reales. Lo decide una persona.
  POSIBLE_DUPLICADO
}

model Aviso {
  id       String      @id @default(cuid())
  tipo     TipoAviso
  // Área del caso: un usuario restringido solo ve los avisos de la suya.
  area     AreaTrabajo @default(POSVENTA)
  // Provincia del caso: un usuario acotado a una provincia solo ve los suyos.
  // null = sin provincia conocida; ese aviso lo ve todo el mundo.
  sucursal String?
  casoId   String?
  caso     Caso?       @relation(fields: [casoId], references: [id], onDelete: Cascade)
  rqrId    String?
  rqr      RQR?        @relation(fields: [rqrId], references: [id], onDelete: Cascade)
  titulo   String
  detalle  String      @db.Text

  creadoEn   DateTime  @default(now())
  // Se apaga a mano ("Marcar visto") o solo cuando el RQR asociado se cierra.
  vistoEn    DateTime?
  vistoPorId String?
  vistoPor   Usuario?  @relation("AvisoVistoPor", fields: [vistoPorId], references: [id])

  @@index([area, vistoEn])
  @@index([sucursal])
  @@index([casoId])
  @@index([rqrId])
}

// ---------------------------------------------------------------------------
// Encuestas de fábrica de Volkswagen
// ---------------------------------------------------------------------------
// Fábrica manda la encuesta (SSI / OSI / CEM) directo al cliente por mail y
// publica un Excel de PENDIENTES: los que todavía no la contestaron. La agencia
// no puede escribirle a esos clientes —el archivo no trae teléfono, solo mail—
// así que el recordatorio va al VENDEDOR que hizo la entrega, para que él los
// llame. Por eso esto NO crea Casos: un Caso vive del circuito de WhatsApp y
// estos clientes nunca van a poder entrar ahí. Es una lista aparte.

enum EstadoEncuestaFabrica {
  PENDIENTE // vino en el último archivo: todavía no contestó
  // Ya se le mandó el mail a su vendedor. Existe para que a nadie le llegue el
  // mismo cliente dos veces: el aviso sale SOLO de los PENDIENTE, así que apenas
  // se avisa el cliente sale de esa bolsa y el próximo mail no lo repite.
  AVISADO
  // SOLO en Encuestas de fábrica PV (pedido del 22-09-2026): acá no hay un
  // vendedor al que avisarle, al cliente lo trabaja Calidad. AVISADO es "se le
  // avisó al cliente y todavía no contestó" y PRIMER_CONTACTO es "ya se habló con
  // él". Los dos cuentan como CONTACTADO en los gráficos. Hasta el 22-09 el
  // "Primer contacto" de PV se guardaba en AVISADO; la migración los pasó acá.
  PRIMER_CONTACTO
  RESPONDIO // dejó de venir en el archivo, así que ya contestó
  // SOLO en Encuestas de fábrica PV (pedido del 19-09-2026): se dejó de trabajar
  // al cliente sin que respondiera. Cuenta como contactado sin respuesta en los
  // gráficos. NO es lo mismo que "mes cerrado" (cerradoEn): eso es el cierre del
  // período; esto es el estado de UN cliente. Ventas no lo usa y lo rechaza.
  CERRADO
}

// Vendedor de VW. NO es un usuario del sistema: no entra, no tiene contraseña.
// Solo existe para saber a qué correo mandarle sus clientes pendientes.
// El código son 7 dígitos: 4 de sucursal (1035 Mendoza, 1036 San Juan) + 3 del
// vendedor. La hoja "Codigos Vendedores" del Excel trae solo los 3 últimos.
model VendedorVW {
  id             String  @id @default(cuid())
  codigo         String  @unique // "1035017"
  codigoSucursal String // "1035"
  numero         Int // 17
  sucursal       String // "MENDOZA"
  // Sale de la hoja de nombres del Excel. Puede faltar: hay códigos que se usan
  // como mostrador (el 002 de cada sucursal vende todos los planes de ahorro) y
  // no figuran en esa hoja. En ese caso lo completa Calidad a mano.
  nombre         String?
  // A dónde se le manda el recordatorio. Sin esto no se le puede avisar.
  email          String?
  // Se apaga en vez de borrarse: si el vendedor se va, sus clientes históricos
  // tienen que seguir teniendo a quién estar asociados.
  activo         Boolean @default(true)

  creadoEn      DateTime  @default(now())
  actualizadoEn DateTime  @updatedAt
  ultimoAvisoEn DateTime? // última vez que se le mandó el mail con su lista

  pendientes EncuestaFabricaVW[]

  @@index([codigoSucursal])
  @@index([activo])
}

// Un cliente que le debe la encuesta a fábrica.
model EncuestaFabricaVW {
  id String @id @default(cuid())

  // El chasis identifica la unidad y no se repite: es la clave para saber si una
  // fila del archivo nuevo es la misma de antes o una entrega distinta.
  chasis  String                @unique
  dominio String?
  estado  EstadoEncuestaFabrica @default(PENDIENTE)

  nombreCliente String
  email         String
  // El teléfono del cliente, para que el vendedor pueda llamarlo (25-09-2026).
  // NO viene en el Excel de fábrica: se busca en los casos de Contacto por el
  // dominio al cargar la encuesta y al avisarle al vendedor (ver
  // services/telefono-cliente.service.ts). null = no se encontró.
  telefono      String?
  fechaEntrega  DateTime?
  // La columna "Fecha Dominio" del Excel de fábrica: cuándo se patentó la unidad.
  // Es la que decide de qué MES es el cliente en el seguimiento de las
  // animaciones. null en los que no la traen: el formato interno, los cargados a
  // mano y los que ya estaban antes de que se leyera.
  fechaDominio  DateTime?
  // El mes del cliente, "AAAA-MM". Sale de fechaDominio y, si falta, de la
  // entrega. Cuando sale de la entrega es una ESTIMACIÓN y fechaDominio queda en
  // null: en el Excel real 6 de 87 clientes patentaron en un mes y recibieron el
  // auto en el siguiente.
  periodo       String?

  // "TRADICIONAL" / "AUTOAHORRO" tal como vienen, más el área que se deduce de
  // ahí. Se guarda el texto original por si fábrica agrega un canal nuevo.
  canalVentas String?
  area        String? // VENTAS | PLAN_DE_AHORRO (catálogo de VW)

  vendedorId String
  vendedor   VendedorVW @relation(fields: [vendedorId], references: [id])
  sucursal   String

  // Columnas de control de fábrica que vinieron completas: si hay algo acá, esa
  // fila fábrica la observó (mail inválido, chasis repetido, etc.).
  observacionesFabrica String[]

  // De qué carga salió y en cuál se la vio por última vez. La segunda es la que
  // decide: si una carga nueva no la trae, es que el cliente ya contestó.
  // Opcional: las cargadas a mano no salen de ningún Excel.
  origenUploadId  String?
  origenUpload    ExcelUpload? @relation(fields: [origenUploadId], references: [id])
  vistaEnUploadId String?

  // Cargada a mano por Calidad. Se EXCLUYE de la barrida que da por respondido
  // todo lo que no venga en el Excel nuevo: como nunca va a venir en ese
  // archivo, sin esto desaparecería sola en la siguiente carga.
  esManual Boolean @default(false)

  // De qué archivo salió: "FABRICA" (el que baja de la plataforma de VW) o
  // "INTERNO" (el export de la concesionaria). Los dos alimentan la misma lista,
  // pero la barrida que da por respondidos SOLO puede cerrar a los de su propio
  // origen: si no, una carga del interno cerraría en silencio a todos los de
  // fábrica.
  origenFormato String @default("FABRICA")

  detectadaEn  DateTime  @default(now())
  avisadoEn    DateTime? // cuándo salió el ÚLTIMO mail al vendedor con este cliente adentro
  // Cuántas veces se le avisó al vendedor por este cliente. Desde el 18-09-2026 se
  // puede volver a pasar a pendiente a los avisados que no respondieron, para
  // recordárselos: con esto el mail distingue los clientes nuevos de los que ya se
  // le habían avisado ("recordatorio"), y la pantalla muestra cuántos avisos van.
  vecesAvisado Int       @default(0)
  respondioEn  DateTime?

  // Lo que carga Calidad a mano cuando el cliente contesta. La calificación va de
  // 1 a 5, la misma escala que la encuesta de posventa por WhatsApp, para que un
  // 4 signifique lo mismo en todo el sistema.
  //
  // Queda como Int? y no como un número obligatorio a propósito: se puede marcar
  // RESPONDIDO sin nota. Pasa seguido —el cliente contestó pero nadie anotó
  // cuánto puso, o fábrica todavía no publicó el puntaje— y obligar a inventar un
  // número sería peor que no tenerlo: ensuciaría los promedios con datos falsos.
  calificacion       Int?
  // El comentario que el cliente le hizo a fábrica, o cualquier cosa a tener en
  // cuenta sobre este caso.
  observacionCalidad String?

  // Cuándo quedó en un MES CERRADO (ver CierrePeriodo). Con fecha, el cliente sale
  // de la lista de trabajo y de los avisos, y solo se consulta; los gráficos lo
  // siguen contando. null = está en la lista.
  cerradoEn DateTime?

  @@index([estado, vendedorId])
  @@index([sucursal, estado])
  @@index([origenFormato])
  @@index([vistaEnUploadId])
  @@index([periodo])
  @@index([cerradoEn])
}

// ---------------------------------------------------------------------------
// Encuesta de Posventa por ítems (Volkswagen)
// ---------------------------------------------------------------------------
// Posventa pasa por varias manos —quien atiende, quien organiza, quien repara,
// quien lava— y con una sola nota no se sabe cuál falló. Se mide cada una por
// separado, en filas distintas, para poder promediar y rankear por ítem sin
// pelear con un JSON.

enum ItemPosventa {
  TRATO
  ORGANIZACION
  CALIDAD_REPARACION
  LAVADO
  GENERAL
}

model EvaluacionPosventa {
  id     String       @id @default(cuid())
  casoId String
  caso   Caso         @relation(fields: [casoId], references: [id], onDelete: Cascade)
  item   ItemPosventa

  // 1 a 5 (5 = mejor). NULL = el cliente no contestó ESE ítem: es distinto de un
  // 1, y mezclarlos ensuciaría el promedio del área.
  estrellas  Int?
  // Lo que el cliente dijo puntualmente de este ítem, si dijo algo. Es lo que
  // convierte un "3" en algo accionable ("el lavado dejó que desear").
  comentario String? @db.Text

  // De qué análisis salió, para poder rastrear la respuesta original.
  analisisId String?
  analisis   SentimentAnalysis? @relation(fields: [analisisId], references: [id], onDelete: SetNull)

  creadoEn DateTime @default(now())

  // Un puntaje por ítem por caso: si el cliente contesta de nuevo, se corrige el
  // que había en vez de sumar otro y contarlo dos veces en el promedio.
  @@unique([casoId, item])
  @@index([item, estrellas])
  @@index([analisisId])
}

/// Encuestas de fábrica de POSVENTA (Volkswagen, pedido de Calidad del 16-09-2026).
///
/// Los clientes de Posventa de la sucursal configurada (marca.encuestaFabricaPV)
/// que calificaron con 5 estrellas: los promotores. Calidad los anima a responder
/// la encuesta de fábrica, con el mismo circuito que las encuestas de Ventas
/// (EncuestaFabricaVW) y el mismo enum de estados. En pantalla AVISADO se lee
/// "Animado": acá no hay vendedor al que avisarle, lo contacta Calidad.
///
/// No es una copia del caso: los datos del cliente se leen del Caso. La lista se
/// sincroniza contra el análisis principal (ver services/encuesta-pv.service.ts).
model EncuestaFabricaPV {
  id String @id @default(cuid())

  // Un caso entra una sola vez: el índice único es lo que hace segura la
  // sincronización cuando dos pedidos la corren a la vez.
  casoId String @unique
  caso   Caso   @relation(fields: [casoId], references: [id], onDelete: Cascade)

  estado EstadoEncuestaFabrica @default(PENDIENTE)

  // Cuándo quedó en 5 el análisis que lo trajo (la respuesta, la llamada o la
  // corrección de Calidad).
  calificadoEn DateTime?
  // Cuándo entró a la lista.
  detectadaEn  DateTime  @default(now())
  // Cuándo Calidad lo animó. Como en Ventas, no se borra al pasar a Respondió: es
  // lo que usa el seguimiento para medir la efectividad.
  animadoEn    DateTime?
  respondioEn  DateTime?

  // Mes cerrado (ver CierrePeriodo). A diferencia de Ventas, acá el mes no se
  // guarda: sale de las fechas del caso, que se pueden corregir. Por eso se anota
  // en qué mes quedó cerrado, y la consulta y la reapertura usan ese.
  cerradoEn     DateTime?
  periodoCierre String?

  @@index([estado])
  @@index([cerradoEn])
}

// ---------------------------------------------------------------------------
// Cierre de meses (Volkswagen, 17-09-2026)
// ---------------------------------------------------------------------------
// Los clientes de un mes se CIERRAN: salen de la lista de trabajo (ocupaban lugar
// y confundían) y quedan para consultar. Se cierra por mes y por provincia, con un
// botón (Calidad) o solo el mes siguiente: el día 19 en Ventas y el 25 en PV (ver
// DIA_CIERRE_AUTOMATICO en services/cierre-periodo.service.ts). Reabre un administrador.

enum ListaCierre {
  ENCUESTA_VENTAS // Encuestas de fábrica (EncuestaFabricaVW)
  ENCUESTA_PV // Encuestas de fábrica PV (EncuestaFabricaPV)
}

enum OrigenCierre {
  MANUAL
  AUTOMATICO
}

model CierrePeriodo {
  id       String       @id @default(cuid())
  lista    ListaCierre
  periodo  String // "AAAA-MM"
  sucursal String // como la escribe la marca: "Mendoza", "San Juan"
  origen   OrigenCierre

  cerradoEn        DateTime @default(now())
  cerradoPorId     String?
  cerradoPorNombre String?
  // Cuántos clientes quedaron cerrados en este mes (se recuenta en cada cierre).
  clientesCerrados Int      @default(0)

  // Un administrador lo reabrió. La fila queda: el cierre automático la ve y no
  // vuelve a cerrar un mes que alguien abrió a propósito.
  reabiertoEn        DateTime?
  reabiertoPorId     String?
  reabiertoPorNombre String?

  @@unique([lista, periodo, sucursal])
}

// ---------------------------------------------------------------------------
// Indicadores CEM por trimestre (Volkswagen, 17-09-2026)
// ---------------------------------------------------------------------------
// Reemplaza la planilla "Q 2026" de Calidad. Los números se cargan a mano (vienen
// de fábrica y del DMS, no del sistema); los porcentajes, totales y la comparación
// con los objetivos los calcula el sistema (indicadores-cem.service.ts).

// Ventas o Posventa (pedido del 18-09-2026: "que se puedan cargar las dos áreas").
// Posventa usa las MISMAS columnas y cuentas, adaptadas a taller: "patentamientos"
// son sus órdenes de reparación, y no se separa tradicional/autoahorro (eso es de
// ventas, plan de ahorro). Lo que ya estaba cargado es todo de Ventas.
enum AreaCem {
  VENTAS
  POSVENTA
}

model IndicadorCemMes {
  id       String  @id @default(cuid())
  periodo  String // "AAAA-MM"
  sucursal String // "Mendoza", "San Juan"
  area     AreaCem @default(VENTAS)

  patentamientos     Int?
  baseCem            Int?
  baseCemTradicional Int?
  baseCemAutoahorro  Int?
  encuestasEfectivas Int?
  baseSinDuplicados  Int?
  mailOk             Int?
  // OS del mes, como lo publica fábrica (4,86).
  os                 Float?

  // POSVENTA (19-09-2026): las columnas de la planilla "Postventa Q 2026". Posventa
  // no usa las de arriba salvo encuestasEfectivas; Ventas no usa estas.
  mailsEnviados         Int?
  notaTrato             Float? // Q1 – Trato
  notaOrganizacion      Float? // Q2 – Organización
  notaCalidadReparacion Float? // Q3 – Calidad de reparación
  notaLvs               Float? // Q4 – LVS (satisfacción general): de ella sale la escala

  actualizadoEn        DateTime @updatedAt
  actualizadoPorNombre String?

  @@unique([periodo, sucursal, area])
}

model IndicadorCemTrimestre {
  id        String  @id @default(cuid())
  anio      Int
  trimestre Int // 1 a 4
  sucursal  String
  area      AreaCem @default(VENTAS)

  // El OS del trimestre lo publica fábrica: NO es el promedio de los tres meses.
  os                 Float?
  // "Resultado auditoría", en porcentaje (88,10).
  resultadoAuditoria Float?

  // POSVENTA: las notas Q1 a Q4 del trimestre las publica fábrica (NO son el
  // promedio de los meses: decisión del 19-09-2026, se cargan a mano).
  notaTrato             Float?
  notaOrganizacion      Float?
  notaCalidadReparacion Float?
  notaLvs               Float?

  actualizadoEn        DateTime @updatedAt
  actualizadoPorNombre String?

  @@unique([anio, trimestre, sucursal, area])
}

// Los objetivos cambian de un trimestre a otro y valen para las dos provincias.
// Cada área tiene los suyos (decisión del 18-09-2026).
model ObjetivoCemTrimestre {
  id        String  @id @default(cuid())
  anio      Int
  trimestre Int
  area      AreaCem @default(VENTAS)

  os          Float?
  cargas      Float? // % de carga mínimo (base CEM / patentamientos)
  mailValidos Float? // % de mails válidos mínimo

  // POSVENTA: la tabla "OBJETIVOS LVS". Desde qué nota LVS empieza cada escala
  // (4,88 / 4,85 / 4,82 / 4,80); por debajo de la 4 es Escala 5 = No cumple.
  lvsEscala1 Float?
  lvsEscala2 Float?
  lvsEscala3 Float?
  lvsEscala4 Float?

  actualizadoEn        DateTime @updatedAt
  actualizadoPorNombre String?

  @@unique([anio, trimestre, area])
}
